// ignore_for_file: constant_identifier_names

enum TypeOfAlert {
  ERROR,
  SUCCESS,
  INFO,
  WARRING,
}

enum ImageType {
  SVG_HTTP,
  SVG_ASSET,
  SVG_FILE,
  SVG,
  NETWORK,
  ASSET,
  FILE,
  ICON,
  NOT_IMAGE,
}

enum TabTool { whistle, clicker, challenge }
