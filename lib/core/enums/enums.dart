// ignore_for_file: constant_identifier_names

enum TypeOfAlert {
  ERROR,
  SUCCESS,
  INFO,
  WARRING,
}

enum ImageType {
  SVG,
  IMAGE,
  NOT_IMAGE,
}

enum ImageUrlType {
  NETWORK,
  ASSET,
  FILE,
  ICON,
  IMAGE_CIRCLE,
}

enum TabTool { whistle, clicker, challenge }
